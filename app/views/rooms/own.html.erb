<h3>登録済み一覧</h3>

  <table>
    <thead>
      <tr>
        <th>画像</th>
        <th>施設名</th>
        <th>紹介文</th>
        <th>料金</th>
        <th>作成日</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
    
      <% @rooms.each do |room| %>
        <tr>
          <td><%= room.image %></td>
          <td><%= room.name %></td>
          <td><%= room.detail %></td>
          <td><%= number_to_currency(room.charge, :unit => "￥",:precision => 0) %>~</td>
          <td><%= room.created_at.strftime("%Y/%m/%d")  %></td>
          <td><%= link_to(room_path(room.id)) do %>詳細はこちら<% end %></td>
        </tr>
      <% end %>
    </tbody>
  </table>